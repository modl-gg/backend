# Shared nginx http-context snippet for Modl realtime WebSocket proxying.
# Install/include this once before vhosts that use $connection_upgrade.

map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}
